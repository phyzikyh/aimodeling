# 3. 코딩실습 {#sec-ch1-lab}

이번 실습에서는 교재 1장의 예제 코드로 NumPy 배열과 PyTorch 텐서의 선형 연산, 자동미분, 브로드캐스팅, 활성화 함수와 연산 장치를 확인합니다.

## 실습 준비

```python
import numpy as np
import torch
import matplotlib.pyplot as plt

print("PyTorch version:", torch.__version__)
print("CUDA available:", torch.cuda.is_available())
```

## 예제 1. NumPy와 PyTorch의 선형 연산

교재 `1_7.py`의 [프로그램 1-3], [프로그램 1-4]처럼 두 라이브러리에서 $y=x^T w+b$를 계산하고 PyTorch 자동미분으로 기울기를 구합니다.

```python
x_np = np.array([1.0, 2.0, 3.0])
w_np = np.array([0.5, 0.2, -0.3])
b_np = 0.1
print("NumPy output:", x_np @ w_np + b_np)

x = torch.tensor([1.0, 2.0, 3.0])
w = torch.tensor([0.5, 0.2, -0.3], requires_grad=True)
b = torch.tensor(0.1, requires_grad=True)
y = x @ w + b
y.backward()
print("PyTorch output:", y.item())
print("grad w:", w.grad)
print("grad b:", b.grad)
```

`y`를 각 가중치로 미분하면 입력값이 되므로 `w.grad`는 `x`와 같습니다. 편향은 결과에 1배로 더해지므로 `b.grad`는 1입니다.

## 예제 2. 브로드캐스팅

교재 [프로그램 1-5]처럼 행렬의 각 행에 같은 벡터를 더합니다.

```python
A_np = np.array([[1., 2., 3.], [4., 5., 6.]])
offset_np = np.array([1., 0., -1.])
print("NumPy:\n", A_np + offset_np)

A = torch.tensor([[1., 2., 3.], [4., 5., 6.]])
offset = torch.tensor([1., 0., -1.])
print("PyTorch:\n", A + offset)
```

## 예제 3. ReLU와 결과 시각화

교재 [프로그램 1-6], [프로그램 1-8]을 연결해 선형 변환과 ReLU 적용 결과를 비교합니다.

```python
x = torch.linspace(-3, 3, steps=100)
y_linear = x + 0.5
y_relu = torch.relu(y_linear)

plt.figure(figsize=(6, 4))
plt.plot(x.numpy(), y_linear.numpy(), label="Linear output")
plt.plot(x.numpy(), y_relu.numpy(), label="ReLU output")
plt.axhline(0, color="gray", linestyle="--", linewidth=0.8)
plt.xlabel("Input x"); plt.ylabel("Output")
plt.title("Linear Transformation and ReLU Activation")
plt.legend(); plt.grid(True); plt.show()
```

## 예제 4. CPU·GPU 자동 선택

교재 [프로그램 1-7]처럼 사용 가능한 장치를 고르고 모든 텐서를 같은 장치에 생성합니다.

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
print("using device:", device)
x = torch.tensor([1.0, 2.0, 3.0], device=device)
w = torch.tensor([0.5, 0.2, -0.3], device=device, requires_grad=True)
b = torch.tensor(0.1, device=device, requires_grad=True)
print("result:", (x @ w + b).item())
```

## 실습문제

다음 5개 문제의 `TODO`를 완성하고 실행 결과와 해석을 제출하세요.

### 문제 1. 텐서의 통계량과 모양 변경

1부터 12까지의 텐서를 `(3, 4)`로 바꾸고 각 열의 평균을 출력하세요.

```python
values = torch.arange(1, 13, dtype=torch.float32)
matrix = # TODO: (3, 4)로 변경
column_mean = # TODO: 각 열의 평균
print(matrix); print(column_mean)
```

### 문제 2. 행렬 곱과 요소별 곱 비교

요소별 곱과 행렬 곱을 각각 계산하고, 결과의 값이 다른 이유를 설명하세요.

```python
A = torch.tensor([[1., 2.], [3., 4.]])
B = torch.tensor([[2., 0.], [1., 2.]])
elementwise = # TODO
matmul = # TODO
print("elementwise:\n", elementwise)
print("matmul:\n", matmul)
```

### 문제 3. 브로드캐스팅으로 표준화

각 열에서 평균을 빼고 표준편차로 나눈 뒤 열별 평균과 표준편차를 확인하세요.

```python
X = torch.tensor([[1., 10., 100.], [2., 20., 120.],
                  [3., 30., 140.], [4., 40., 160.]])
mean = # TODO: 열별 평균
std = # TODO: 열별 표준편차
standardized = # TODO
print(standardized.mean(dim=0))
print(standardized.std(dim=0))
```

### 문제 4. 자동미분으로 기울기 확인

$L=(wx+b-y_{true})^2$의 기울기를 자동미분으로 구하세요.

```python
x = torch.tensor(2.0); y_true = torch.tensor(4.0)
w = torch.tensor(0.5, requires_grad=True)
b = torch.tensor(0.1, requires_grad=True)
y_pred = # TODO
loss = # TODO
# TODO: 역전파 실행
print("loss:", loss.item(), "grad_w:", w.grad.item(), "grad_b:", b.grad.item())
```

### 문제 5. 활성화 함수 비교 시각화

`[-5, 5]`에서 ReLU, Sigmoid, Tanh를 한 그래프에 그리고 출력 범위와 음수 입력 처리 차이를 3문장 이내로 설명하세요.

```python
x = torch.linspace(-5, 5, steps=200)
y_relu = # TODO
y_sigmoid = # TODO
y_tanh = # TODO
# TODO: 세 곡선과 범례, 축 이름, 격자 추가
plt.show()
```

**제출물**: 완성한 노트북 또는 `.py` 파일, 문제 2·5의 설명, 모든 실행 결과와 그래프.

> 예제 출처: 김태훈, 『IT CookBook, 파이토치로 배우는 현대 인공지능』, 한빛아카데미(2026), 제공 코드 `1_3.py`, `1_7.py`를 강의 흐름에 맞게 발췌·재구성.
